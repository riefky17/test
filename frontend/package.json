{
  "name": "family-app-suite-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@family-app-suite/finance-app && npm run build --workspace=@family-app-suite/geochat-app && npm run build --workspace=@family-app-suite/fitness-app"
  }
}
